const { BookingRepository } = require('../repository/index');

class BookingService {
    async create (data) {
        try {
            
        } catch (error) {
            
        }
    }
}

module.exports = BookingService;